server:
  port: 8888

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user?serverTimezone=Asia/Seoul&allowMultiQueries=true
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver

  devtools:
    restart:
      enabled: true

  application:
    name: rest-sample-app-service

  main:
    allow-bean-definition-overriding: true

#  h2:
#    console:
#      enabled: true
#      path: h2-console

mybatis:
  type-aliases-package: com.example.springbootmybatis.entity
  mapper-locations: mapper/**/*.xml

